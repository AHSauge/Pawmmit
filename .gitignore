build
.cache
.DS_Store
.project
.vscode/
build*
.idea/
.venv
compile_commands.json
.flatpak-builder

# Meson wrap checkouts / cache (everything under subprojects/ except the
# tracked .wrap files and packagefiles/ overlays).
/subprojects/*/
!/subprojects/packagefiles/
/subprojects/packagecache/
/subprojects/.wraplock
